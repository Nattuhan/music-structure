class Measure extends AudioWorkletProcessor {constructor(){super();this.last=Array(6).fill(-1);}process(inputs,outputs){for(let ch=0;ch<inputs[0].length;ch++)for(let i=0;i<inputs[0][ch].length;i++){const t=(currentFrame+i)/sampleRate;if(Math.abs(inputs[0][ch][i])>.08&&t-this.last[ch]>.25){this.last[ch]=t;this.port.postMessage({ch,t});}}return true;}}registerProcessor('measure',Measure);